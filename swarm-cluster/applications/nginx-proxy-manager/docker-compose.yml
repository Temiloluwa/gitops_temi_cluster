version: '3.8'
services:
  app:
    image: 'jc21/nginx-proxy-manager:latest'
    restart: unless-stopped
    ports:
      - '80:80' 
      - '443:443' 
      - '81:81' 
    deploy:
      replicas: 1
    
    volumes:
      - ./npm-data:/data
      - ./npm-letsencrypt:/etc/letsencrypt  
      